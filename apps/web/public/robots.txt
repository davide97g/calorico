# Calorico — https://calorico.davideghiotto.it
#
# The app is the site root. Signed out it renders a sign-in screen, which is all
# a crawler will ever see of it — there is no landing page to index instead. The
# routes below the root serve the same shell as `/` and say so with a canonical,
# so there is no reason to spend a crawl budget on them.

User-agent: *
Allow: /$
Allow: /privacy
Allow: /termini
Disallow: /api/

# AI crawlers are welcome on the public pages: being quoted accurately in an
# answer is the point. /llms.txt is the short version written for them.
User-agent: GPTBot
Allow: /$
Allow: /privacy
Allow: /termini
Disallow: /api/

User-agent: OAI-SearchBot
Allow: /$
Allow: /privacy
Allow: /termini
Disallow: /api/

User-agent: ClaudeBot
Allow: /$
Allow: /privacy
Allow: /termini
Disallow: /api/

User-agent: PerplexityBot
Allow: /$
Allow: /privacy
Allow: /termini
Disallow: /api/

User-agent: Google-Extended
Allow: /$
Allow: /privacy
Allow: /termini
Disallow: /api/

Sitemap: https://calorico.davideghiotto.it/sitemap.xml
